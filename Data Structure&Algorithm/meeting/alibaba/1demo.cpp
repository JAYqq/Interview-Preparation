#include<iostream>
using namespace std;
int dfs(int k,int level,vector<int> order){
    if(level==m):
        for(int i=0;i<order.size();i++)
        {
            if(i==order.size())
                cout<<order
            cout<<order[i]<<" ";
        }
            print(item,end=" ")
        print("\n")
        return
        for i in range(k,n+1):
            order.append(i)
            dfs(i+1,level+1,order)
            order.pop()
}